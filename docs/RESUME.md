# PC 종료 전 저장 — 2026-09-15

## 재개 후 진행

사용자가 재개를 요청하여 회사 사이트 통합 작업을 진행했다. `site.js`와 `site.css`의 정적 읽기·모바일·짧은 화면·동작 줄이기·숨겨진 링크 접근을 보완했다. `scripts/qa.cjs`의 24가지 조합이 통과했고 세 종류의 화면을 직접 확인했다. 상세 결과는 `QA.md`, 독립 검토 상태는 `REVIEW.md`를 따른다. 아래 중단 문구와 미적용 패치 기록은 이전 체크포인트의 이력이다. 다음 작업은 최종 영상, 실제 제품 화면, 이미지 전송 성능 및 서비스 운영 준비다. 공개 배포는 아직 하지 않았다.

2026-09-16 재개 후 PR2에 CI를 추가하는 작업을 진행한다. 범위와 결과는 [CI-READINESS.md](CI-READINESS.md)를 따른다. 아래 PC 종료 및 예전 미완료 기록은 이력이다. 서비스 보고서 여정 후속은 zlalxp/esg-agent PR209/004d192이며, 회사 제품 이미지 촬영 기준4815219와 구분한다.

## 현재 작업

- 저장소: zlalxp/esgology
- 작업 브랜치: feat/company-scroll-20260915
- 출발점: fix/company-copy-20260915 / 91111507da5893d345f150bfd7478a160df562d1. 기존 문구 브랜치와 Draft PR1을 보존했다.
- 별도 company-launch-preview-20260915 시안의 index.html, site.css, site.js, 생성 이미지 PNG 4장 및 image-provenance.json을 가져왔다.
- 홈페이지에 main 식별자, 기존 제품·문의·채용 페이지 링크, 자바스크립트 비활성화 시 문의·이미지 출처 안내를 추가했다.
- 원본 시안은 수정하지 않았다. 현재 브라우저에 열려 있는 원본 시안과 이 통합 브랜치는 서로 다른 파일이다.

## 검증 경계

원본 시안은 앞선 작업에서 화면 크기 4종과 동작 줄이기 설정을 검사했다. 이번 통합 브랜치에서는 아직 브라우저 QA와 Claude 독립 검토를 수행하지 않았다. git diff --check만 통과했다. 이 저장은 완료된 출시 후보가 아니라 중간 체크포인트다.

site.js 전체 교체와 CSS 보완 패치는 동일 파일에 삭제·추가를 함께 지정하여 도구가 거절했다. 해당 호출은 실패했고, JS 및 CSS는 가져온 원본 그대로임을 재확인했다. 패치 일부가 적용된 것으로 간주하지 않는다.

## 다음 실행 순서

1. 이 문서와 SCROLL-INTEGRATION.md를 읽고 Git 상태를 확인한다.
2. JS가 없을 때 모든 단계 본문이 보이도록 기본 정적 레이아웃을 보완한다. 짧은 화면, 모바일, 동작 줄이기 모드와 모드 전환도 확인한다. 스크롤로 투명해진 링크의 키보드 접근 문제를 점검한다.
3. 개인 경로에 의존하지 않는 검증 스크립트를 작성한다. 모바일·데스크톱, 키보드·문의 창·Escape, 링크, 외부 요청, 이미지 로딩, JS 비활성화 검증과 화면 캡처를 남긴다.
4. 실제 변경 파일을 Claude에 전달해 읽기 전용 독립 검토를 받는다. Grok은 이전 실행에서 설명만 반환하고 패치를 만들지 못한 사례가 반복되어 현재 작업에 다시 배정하지 않았다.
5. 수정 및 검증 후 검토용 PR을 만들고 사용자가 클릭할 실제 시안을 연다. main 병합, Pages 배포, DNS 변경은 이번 중단 전 수행하지 않았다.

## 남은 콘텐츠

생성 이미지는 실제 회사 시설이나 고객 사진이 아니다. 최종 영상 제작은 사용자 요청에 따라 추후 진행한다. 제품 그림은 합성 데이터이며 실제 서비스 화면으로 교체해야 한다. PNG 4장의 총 용량은 약 8.8MB로 공개 전 인코딩·전송 성능을 점검한다. 로고 상표권 검토 완료를 주장하지 않는다.

## 서비스 개발 인수인계

별도 zlalxp/esg-agent의 ops/pilot-intake-20260915 브랜치와 docs/handoff/2026-09-15-gpt/README.md, STATUS.md, PILOT-PG18-FULL-SUITE.md를 따른다. 직전 서비스 체크포인트 d355904, 검증 코드 4815219: PR207 9개 검사 성공. PG16·PG18 각각 전체 2678개 및 별도 TLS 3개 통과. 실제 운영 배포와는 별개다.

운영 준비는 DB 권한 분리, DB·파일 백업 및 격리 복원, 같은 코드 버전의 web/worker, 파일럿 동의·허용·예산 기록, 도메인 E2E가 남아 있다. 직전 브라우저 도구는 실행 경로 오류, VPS 직접 SSH는 인증 거절이었다. 운영 접근 경로 질문은 답변 대기 중이며 비밀키나 DB 주소를 채팅에 요구하지 않는다.

C++ Build Tools 설치와 Graft 평가는 이미 완료했다. Graft 구조 요약은 표본 토큰 82.3% 감소, 검색 6건은 rg보다 39.6% 증가했다. 전체 비용 절감률로 일반화하지 않는다. 서비스 저장소 GRAFT-EVALUATION.md 참조.

중단 시 이번 작업이 시작한 빌드·개발 서버·원격 에이전트는 없다. 사용자나 다른 세션이 띄운 프로세스는 종료하지 않는다.
